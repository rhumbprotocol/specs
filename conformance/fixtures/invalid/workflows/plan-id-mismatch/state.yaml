# Negative fixture for INV-1 (plan_id mismatch).
# state.yaml's plan_id intentionally differs from PLAN.md's plan_id.

plan_id: MP-1111-state-side
title: "Plan ID Mismatch (negative fixture)"
rwp_version: "0.27.0"

execution:
  status: in_progress
  current_phase: P-01

phases:
  P-01:
    title: "Sole phase"
    status: in_progress
